Library distribution thorns and ID reader thorns for initial data generated by Elliptica.
These thorns have been compiled with the EinsteinToolkit using the thornlist found in 
     utils/elliptica.th
and the compilation options found in 
     utils/expanse.cfg

To compile these thorns with the EinteinToolkit, take the following steps:
1. clone this repo into the Cactus/repos/ directory
2. Move to the base Cactus directory, copy the symlinks_env.sh there, and run it
     chmod u+x symlinks_env.sh
     ./symlinks_env.sh
this establishes the correct symbolic links and sets up the environment for compiling
3. Edit the compilation script (simfactory/mdb/optionlists/expanse.cfg) paths for SUITESPARSE_DIR and 
ELLIPTICA_DIR to your local installs or Alireza's installs
     SUITESPARSE_DIR = /home/arashti/Software/Elliptica_packages/SuiteSparse-5.7.2
     ELLIPTICA_DIR = /path/to/Elliptica
4. compile the EinsteinToolkit
     ./simfactory/bin/sim setup-silent
     ./simfactory/bin/sim build elliptica -j64 --thornlist thornlists/elliptica.th --optionlist simfactory/mdb/optionlists/expanse.cfg --machine expanse | tee OUTCOMP

